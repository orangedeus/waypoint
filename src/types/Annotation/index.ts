export type AnnotationMeta = {
    following: boolean
    annotated: number
    boarding: number
    alighting: number
    url: string
    code: string
}

export type Survey = {
    code: string
    name: string
    age: number
    sex: string
    educ: string
}